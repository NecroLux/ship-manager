# Google Sheets API Configuration with Service Account
# Get a service account key from: https://console.cloud.google.com/
# 1. Create a new project in Google Cloud Console
# 2. Create a Service Account
# 3. Create a JSON key for the service account
# 4. Share your Google Sheet with the service account email (found in the JSON key file)
# 5. Save the JSON key file to credentials.json (this file should NOT be committed to git)
# 6. Set the path below to point to your service account key file

GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials.json
PORT=5000

# Frontend configuration (optional)
VITE_BACKEND_URL=http://localhost:5000

