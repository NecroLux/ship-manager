name: Deploy Backend to Railway

on:
  push:
    branches:
      - main
    paths:
      - 'server/**'
      - '.github/workflows/deploy-railway.yml'
      - 'package.json'
      - 'Procfile'

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v3
      
      - name: Notify deployment start
        run: echo "🚀 Building and deploying backend to Railway..."
        
      - name: Deploy to Railway (via GitHub integration)
        run: |
          echo "Railway will automatically deploy when you push to main branch"
          echo "If you haven't set up Railway yet, visit: https://railway.app"
          echo "Connect your GitHub repository in the Railway dashboard"

